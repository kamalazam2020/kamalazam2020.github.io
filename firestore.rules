rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Site content published documents: public can read, authenticated admin can read and write
    match /siteContent/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
